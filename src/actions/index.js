export const companyInputs = (value, property) => ({
  root: 'company',
  type: 'inputs',
  value,
  property,
});

export const userInputs = (value, property) => ({
  root: 'user',
  type: 'inputs',
  value,
  property,
});

export const termsInputs = (value, property) => ({
  root: 'terms',
  type: 'inputs',
  value,
  property,
});

export const companyErrors = (value, property) => ({
  root: 'company',
  type: 'errors',
  value,
  property,
});

export const userErrors = (value, property) => ({
  root: 'user',
  type: 'errors',
  value,
  property,
});

export const termsErrors = (value, property) => ({
  root: 'terms',
  type: 'errors',
  value,
  property,
});
